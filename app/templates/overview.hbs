{{page-title "Overview"}}
<Header @username={{this.username}} @isAuthenticated={{this.isAuthenticated}} />
<AuApp>
  <AuBodyContainer @scroll={{this.scroll}}>
    <Search @username={{this.username}} @apps={{this.apps}} />
    <AuHr class="au-u-margin-none" />
    <AppCards @apps={{this.apps}} />
  </AuBodyContainer>
</AuApp>
<Footer />